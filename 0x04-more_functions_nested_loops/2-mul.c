#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: - First integer
 * @b: - Second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = b * a;
return (result);
}
